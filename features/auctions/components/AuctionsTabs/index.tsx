import React from "react";

type Props = {
  tabs?: string[];
  active?: number;
};

export default function AuctionsTabs({
  tabs = ["القائمة", "القادمة", "المنتقى"],
  active = 0,
}: Props) {
  return (
  <></>
  );
}
