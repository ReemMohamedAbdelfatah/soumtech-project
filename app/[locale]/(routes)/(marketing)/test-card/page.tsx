import PropertyDetailsCard from "@/app/[locale]/(routes)/(marketing)/PropertyDetailsCard";



export default function SandboxPage() {
  return (
    // حذفنا py-12 و px-4 لكي لا يدفعا المحتوى للأسفل
    //<div className="min-h-screen bg-slate-100"> 
      <PropertyDetailsCard />
    //</div>
  );
}